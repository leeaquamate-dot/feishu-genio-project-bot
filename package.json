{
  "name": "feishu-bot-cloudflare",
  "version": "1.0.0",
  "description": "Feishu Bitable monitoring bot deployed on Cloudflare Workers",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "db:migrate": "wrangler d1 execute feishu-bot-backups --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240529.0",
    "typescript": "^5.4.5",
    "wrangler": "^3.57.0"
  }
}
